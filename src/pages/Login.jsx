import React from "react";
import { Button, Card, Center, Container, Flex, Input } from "@mantine/core";
import LoginForm from "../components/Login/LoginForm";

const Login = () => {
  return <LoginForm />;
};

export default Login;
